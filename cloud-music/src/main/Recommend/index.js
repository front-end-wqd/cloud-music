import React from 'react';
import Swiper from '../../component/swiper/index';

function Recommend() {
    return (
        <div>
            <Swiper></Swiper>
        </div>
    );
}

export default React.memo(Recommend);
